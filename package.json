{
  "name": "istanbulite",
  "version": "1.0.0",
  "description": "Istanbulite — referral-gated community platform for Istanbul residents. Static site wrapped with Capacitor for the iOS app.",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "sync": "node scripts/sync-web.js",
    "cap:sync": "npm run sync && npx cap sync ios",
    "cap:open": "npm run sync && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
